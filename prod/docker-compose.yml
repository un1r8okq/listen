version: '3'
services:
    listen:
        build: ../
        environment:
            APP_KEY: REPLACE_WITH_APP_KEY
        ports:
            - '80:80/tcp'
        volumes:
            - 'sqlite:./database/database.sqlite'
volumes:
    sqlite:
        driver: local
